{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "worktop build src/index.ts",
    "dev": "miniflare -w",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "worktop": "^0.7.3"
  },
  "devDependencies": {
    "miniflare": "^2.12.0",
    "nanoid": "^4.0.1",
    "typescript": "^4.9.5",
    "worktop.build": "^0.0.5",
    "wrangler": "^2.10.0",
    "zod": "^3.20.6"
  }
}