{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "worktop build src/index.ts",
    "dev": "miniflare -w",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "worktop": "next"
  },
  "devDependencies": {
    "miniflare": "^2.11.0",
    "nanoid": "^4.0.0",
    "typescript": "^4.7.4",
    "worktop.build": "latest",
    "zod": "^3.19.1"
  }
}